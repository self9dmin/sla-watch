# Dynatrace environment used by dt-app dev/deploy when supplied in the shell.
# The value may also be passed with --environment-url.
DT_APP_ENVIRONMENT_URL=https://your-environment.apps.dynatrace.com/
