{
  "$schema": "./.dt-app/app.config.schema.json",
  "environmentUrl": "https://your-environment.apps.dynatrace.com/",
  "app": {
    "name": "SLA Review",
    "version": "0.0.67",
    "icon": "ui/assets/sla-review-icon.svg",
    "description": "Review provider terms against Dynatrace evidence and service ownership",
    "id": "my.sla",
    "scopes": [
      { "name": "environment-api:credentials:read", "comment": "Read an administrator-selected AppEngine credential for an optional provider incident connection" },
      { "name": "storage:events:read", "comment": "Read problem events that can affect SLA posture" },
      { "name": "storage:metrics:read", "comment": "Read service health signals for SLA evaluation" },
      { "name": "storage:logs:read", "comment": "Check whether correlated log telemetry exists" },
      { "name": "storage:spans:read", "comment": "Check whether distributed tracing telemetry exists" },
      { "name": "storage:smartscape:read", "comment": "Read provider inventory and service-to-runtime topology for provider discovery and contract scope selection" },
      { "name": "slo:slos:read", "comment": "Find objectives previously created from confirmed service coverage" },
      { "name": "slo:slos:write", "comment": "Create one customer-confirmed availability objective for a selected Dynatrace service" },
      { "name": "app-settings:objects:read", "comment": "Read custom terms, provider connections, confirmed scope mappings, and evidence decisions" },
      { "name": "app-settings:objects:write", "comment": "Manage custom terms, scope mappings, provider connections, and human evidence decisions" },
      { "name": "state:user-app-states:read", "comment": "Restore the personal theme preference" },
      { "name": "state:user-app-states:write", "comment": "Save the personal theme preference" },
      { "name": "state:app-states:read", "comment": "Read shared provider review configuration" },
      { "name": "state:app-states:write", "comment": "Save shared provider review configuration" }
    ]
  },
  "plugins": ["@dynatrace-sdk/dt-app-plugin-client-app-settings-v2"]
}
